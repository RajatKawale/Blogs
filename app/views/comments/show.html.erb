<!DOCTYPE html>
<!--[if lt IE 7]>      <html class="no-js lt-ie9 lt-ie8 lt-ie7"> <![endif]-->
<!--[if IE 7]>         <html class="no-js lt-ie9 lt-ie8"> <![endif]-->
<!--[if IE 8]>         <html class="no-js lt-ie9"> <![endif]-->
<!--[if gt IE 8]>      <html class="no-js"> <!--<![endif]-->
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title>Comments</title>
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="">
        <style>
        .righty {
        position: absolute;
        right: 30px;
        }
        .lefty {
        position: absolute;
        left: 30px;
        }
        </style>
    </head>
    <body class="theme">
        <!--[if lt IE 7]>
            <p class="browsehappy">You are using an <strong>outdated</strong> browser. Please <a href="#">upgrade your browser</a> to improve your experience.</p>
        <![endif]-->
        
        <script src="" async defer></script>
        <center><h1><b>Comments</b></h1></center>
        <!--<% @comments.reverse_each do |comment| %>
        <div class="main">
            <h3><i><%= User.find(comment.user_id).name%></i><h3>
            <h4><font color="white"><%= comment.comment %><font></h4>
            <h5 class="lefty"><%= comment.created_at %><h5><%if current_user.id==comment.user_id%><h5 class="righty"><%= link_to "delete", "/comments/#{comment.id}/delete", class: "btn btn-danger"%><h5><%end%>
            <br/>
            <br/>
        </div>
        <br/>
        <%end%>
        <center>
        <form method="post" action="/comments/new">
        <input type="text" style="width:600px" name="comment" placeholder="Enter your comment" value="">
        <button type="submit" class="btn btn-primary">Comment<button>
        </form>
        </center>-->
        <center>
        <% @comments.reverse_each do |comment| %>
        <div class="card" style="width:50%">
            <div class="card-header">
                <h3><i><%= User.find(comment.user_id).name%></i></h3>
            </div>
            <div class="card-body">
                <h4><%= comment.comment %></h4>
                <h5 class="lefty"><%= comment.created_at %><h5><%if current_user.id==comment.user_id%><h5 class="righty"><%= link_to "delete", "/comments/#{comment.id}/delete", class: "btn btn-danger"%><h5><%end%>
                <br/>
            </div>
        </div>
        <br/>
        <%end%>
        </center>
        <center>
        <form method="post" action="/comments/new">
        <input type="text" style="width:600px" name="comment" placeholder="Enter your comment" value="">
        <button type="submit" class="btn btn-primary">Comment<button>
        </form>
        </center>
    </body>
</html>